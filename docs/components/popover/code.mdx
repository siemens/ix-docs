---
doc-type: 'tab-item'
description: 'Code examples and API documentation for the ix-popover'
---

import PropsApi from '@site/docs/autogenerated/api/ix-popover/api.mdx';
import HeaderPropsApi from '@site/docs/autogenerated/api/ix-popover-header/api.mdx';
import ContentPropsApi from '@site/docs/autogenerated/api/ix-popover-content/api.mdx';
import FooterPropsApi from '@site/docs/autogenerated/api/ix-popover-footer/api.mdx';
import ImagePropsApi from '@site/docs/autogenerated/api/ix-popover-image/api.mdx';
import PopoverPlayground from '@site/docs/autogenerated/playground/popover.mdx';

# Popover - Code

## Example

The playground below shows one composed popover using the available properties and slots across all sub-components. Click **Open** to display the panel. **Cancel** calls `hidePopover()` on the parent `ix-popover`.

<PopoverPlayground height="32rem" />

## Structure

```html
<ix-button id="triggerId">Open</ix-button>
<ix-popover trigger="triggerId" placement="bottom" has-spike close-on-click-outside>
  <ix-popover-header icon="info">Popover title</ix-popover-header>
  <ix-popover-image src="…" alt="…"></ix-popover-image>
  <ix-popover-content>Body text</ix-popover-content>
  <ix-popover-footer alignment="vertical">
    <span slot="start">v1.0.0</span>
    <ix-button variant="secondary">Cancel</ix-button>
    <ix-button>Save</ix-button>
  </ix-popover-footer>
</ix-popover>
```

- **Popover title** — default slot text of `ix-popover-header` (see example above).
- **`additional-items` slot** — optional content beside the title (for example `ix-pill`).
- **`start` slot** on `ix-popover-footer` — optional leading footer content.

Dismiss the popover from custom footer actions:

```js
document.getElementById('popover')?.hidePopover();
```

## Methods

| Method | Description |
| --- | --- |
| `showPopover()` | Open the popover programmatically. |
| `hidePopover()` | Close the popover programmatically. |

## Events

| Event | Description |
| --- | --- |
| `showChange` | Fires before visibility changes. Cancel the event to prevent open/close. |
| `showChanged` | Fires after visibility has changed. |
| `closeClick` (`ix-popover-header`) | Fires when the header close button is clicked. Cancel to prevent closing. |

## API

### ix-popover

<PropsApi />

### ix-popover-header

<HeaderPropsApi />

### ix-popover-content

<ContentPropsApi />

### ix-popover-footer

<FooterPropsApi />

### ix-popover-image

<ImagePropsApi />
